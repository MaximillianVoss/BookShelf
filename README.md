# BookShelf

Android-приложение для отслеживания прочитанных книг.

Тема дипломного проекта: **"Приложение для отслеживания прочитанных книг"**.

Приложение позволяет пользователю искать книги из внешних источников, добавлять их в личную библиотеку, менять статус чтения, ставить оценку, вести статистику чтения и получать рекомендации по жанрам уже прочитанных книг.

## Стек

- Язык: Kotlin
- UI: Jetpack Compose
- Платформа: Android
- IDE: Android Studio
- Локальная БД: SQLite через Room
- Сеть: Retrofit + Gson
- Загрузка обложек: Coil
- Тесты: JUnit

## Основные возможности

- Регистрация и вход пользователей.
- Демо-вход с заранее заполненной библиотекой и статистикой.
- Поиск книг по названию, автору или жанру.
- Выбор источника поиска: все источники, Open Library, Gutendex, Internet Archive, Library of Congress, локальный Python-сервер или локальный каталог.
- Быстрый поиск по основным жанрам.
- Кнопка **"Показать еще"** в поиске и рекомендациях с дозагрузкой большего количества результатов.
- Загрузка данных из официальных API без токенов и ключей.
- Резервный локальный каталог, если внешние API временно недоступны.
- Личная библиотека пользователя.
- В общем списке библиотеки книги со статусом `Читаю` выводятся первыми.
- Статусы книг:
  - `Хочу прочитать`
  - `Читаю`
  - `Прочитано`
  - `Отложено`
  - `Брошено`
- Детальный экран книги с описанием, жанрами, источником, оценкой и отзывом.
- Чтение/предпросмотр книги внутри приложения через встроенный WebView.
- Сохранение последней позиции чтения: URL страницы и вертикальная прокрутка восстанавливаются при следующем открытии книги из библиотеки.
- Автоматический учет времени чтения при выходе из встроенного экрана чтения.
- Ручное добавление дня чтения: минуты и страницы.
- Статистика:
  - количество книг по статусам;
  - прочитанные книги;
  - книги в процессе чтения;
  - минуты чтения;
  - страницы;
  - средняя оценка;
  - календарь чтения за 30 дней;
  - популярные жанры.
- Рекомендации на основе жанров прочитанных книг.
- Кеширование рекомендаций в локальной БД: сохраненная подборка показывается повторно, пока пользователь не обновит ее вручную или не запросит больше результатов.

## Демо-пользователь

На экране входа есть кнопка демо-входа. Она автоматически выполняет вход и заполняет поля учетными данными.

```text
Логин:  demo
Пароль: demo1234
```

При демо-входе приложение добавляет тестовые данные:

- 10 книг с разными статусами;
- прочитанные книги с оценками и отзывами;
- 20 сессий чтения за последние 30 дней;
- жанры для проверки рекомендательной системы.

Демо-сессии чтения пересоздаются при каждом демо-входе, поэтому календарь всегда остается актуальным для текущей даты. Пользовательские сессии чтения при этом не удаляются.

## Источники книг

Сейчас используются:

- Open Library API: `https://openlibrary.org/search.json`
- Gutendex API: `https://gutendex.com/books/`
- Internet Archive Advanced Search API: `https://archive.org/advancedsearch.php`
- Library of Congress JSON API: `https://www.loc.gov/books/?fo=json`
- локальный Python-сервер: `http://10.0.2.2:8000/api/books`
- локальный fallback-каталог внутри приложения

В ветке `api-sources-no-key` Google Books отключен. Все внешние источники работают без API-ключа, OAuth, токенов и антибот-проверок. В ветке `local-book-server` дополнительно подключен локальный Python-сервер, который можно запускать на ноутбуке для выдачи карточек книг, обложек и HTML-страниц чтения. В режиме `Все` приложение запрашивает каждый внешний API и локальный сервер отдельно, удаляет дубликаты по названию и первому автору, затем показывает общий список. Если внешние источники недоступны или вернули пустой результат, используется локальный набор книг.

| Источник | Назначение | Что используется в приложении |
|---|---|---|
| Open Library | Общий книжный каталог | название, авторы, год, subjects, язык, обложка, ссылка на страницу |
| Gutendex | Project Gutenberg, public domain книги | название, авторы, subjects, summary, язык, обложка, HTML/TXT-ссылка для чтения |
| Internet Archive | Архив оцифрованных текстов | identifier, title, creator, description, subject, date, language, cover URL |
| Library of Congress | Библиографические записи loc.gov | title, creator/contributor, subject, date, language, image_url, url |
| Наш сервер | Локальный Python-сервис для демо чтения | карточки книг, жанры, PNG-обложка, HTML-ссылка для чтения |
| Локальный каталог | Резервный источник | заранее заданные книги для демо и offline/fallback-сценария |

Выбранный пользователем конкретный источник не подменяется fallback-каталогом. Fallback применяется только для режима `Все` или при явном выборе `Локальный каталог`, чтобы на защите было видно реальное состояние каждого API.

## Локальный Python-сервер

В ветке `local-book-server` добавлен отдельный Python-проект. В его каталоге 132 тестовые книги: 6 базовых записей и 126 сгенерированных книг по основным жанрам приложения.

```text
book_server
├── pyproject.toml
├── README.md
├── book_server
│   ├── catalog.py
│   └── main.py
└── tests
    └── test_catalog.py
```

Запуск сервера:

```powershell
cd book_server
python -m book_server.main
```

После запуска доступны:

- `http://127.0.0.1:8000/health` - проверка сервера;
- `http://127.0.0.1:8000/api/books?q=war&limit=20` - поиск книг;
- `http://127.0.0.1:8000/books/war-and-peace-demo/read` - HTML-читалка.

Android-эмулятор обращается к серверу через специальный адрес `10.0.2.2`, поэтому в приложении базовый адрес задан как:

```text
http://10.0.2.2:8000/
```

На реальном телефоне вместо `10.0.2.2` нужен IP ноутбука в одной Wi-Fi сети.

## Чтение внутри приложения

На экране книги кнопка **"Читать в приложении"** открывает страницу источника во встроенном WebView.

Важно: приложение показывает тот режим чтения или предпросмотра, который предоставляет источник. Например, Open Library может показывать только `Preview Only`, если полный текст недоступен. Для источника `Наш сервер` открывается локальная HTML-страница, которую возвращает Python-сервер.

При выходе из экрана чтения появляется диалог:

```text
Засчитать чтение?
```

Если нажать **"Засчитать"**, приложение:

- рассчитывает время с момента открытия экрана чтения;
- сохраняет минимум 1 минуту, если чтение было короче минуты;
- добавляет запись в `reading_sessions`;
- если книги еще нет в библиотеке, добавляет ее со статусом `Читаю`;
- сохраняет последнюю открытую страницу/позицию прокрутки для следующего открытия;
- обновляет статистику и календарь чтения.

## Рекомендации

Рекомендательная система работает по жанрам прочитанных книг.

Логика:

1. Берутся книги пользователя со статусом `Прочитано`.
2. Из них извлекаются жанры и тематики.
3. Считаются самые частые жанры.
4. По этим жанрам выполняется поиск новых книг.
5. Книги, уже добавленные в библиотеку пользователя, исключаются.
6. Итоговый список сохраняется в локальный кеш рекомендаций.
7. При следующем открытии экрана показывается сохраненная подборка; пользователь может обновить ее вручную.

Для демо-пользователя подборка сразу работает, так как в его библиотеке уже есть прочитанные книги с жанрами `Adventure`, `Fantasy`, `Science Fiction` и другими.

## Архитектура проекта

Проект использует простую MVVM-структуру:

```text
UI Jetpack Compose
        |
        v
BooksViewModel
        |
        v
Repositories
        |
        +--> Room / SQLite
        |
        +--> Retrofit APIs
        |
        +--> Local Python Book Server
```

Основные директории:

```text
app/src/main/java/com/finenkodenis/bookshelf
├── MainActivity.kt
├── BooksApplication.kt
├── data
│   ├── AppContainer.kt
│   ├── Book.kt
│   ├── BookSearchSource.kt
│   ├── BooksRepository.kt
│   ├── LibraryRepository.kt
│   ├── UserRepository.kt
│   ├── RecommendationEngine.kt
│   ├── DemoLibrarySeed.kt
│   ├── FallbackBooks.kt
│   └── local
│       ├── BooksDatabase.kt
│       ├── Entities.kt
│       ├── BookDao.kt
│       ├── UserDao.kt
│       ├── UserBookDao.kt
│       └── ReadingSessionDao.kt
├── network/model
│   ├── GutendexService.kt
│   ├── InternetArchiveService.kt
│   ├── LibraryOfCongressService.kt
│   ├── LocalBookServerService.kt
│   └── OpenLibraryService.kt
└── ui/theme
    ├── BooksApp.kt
    ├── BooksViewModel.kt
    └── screens
        ├── AuthScreen.kt
        ├── SearchScreen.kt
        ├── LibraryScreen.kt
        ├── BookDetailScreen.kt
        ├── ReaderScreen.kt
        ├── RecommendationsScreen.kt
        └── StatsScreen.kt
book_server
├── book_server
│   ├── catalog.py
│   └── main.py
└── tests
    └── test_catalog.py
```

## База данных

Приложение использует Room поверх SQLite.

Файл настройки БД:

```text
app/src/main/java/com/finenkodenis/bookshelf/data/local/BooksDatabase.kt
```

Имя БД:

```text
bookshelf.db
```

Путь на устройстве или эмуляторе:

```text
/data/data/com.finenkodenis.bookshelf/databases/bookshelf.db
```

Таблицы текущей реализации:

```text
users
books
user_books
reading_sessions
recommendation_cache
```

### Текущая Room-схема

```text
┌────────────────────┐
│ users              │
│--------------------│
│ PK user_id         │
│ username           │
│ email              │
│ password_hash      │
│ password_salt      │
│ created_at         │
│ last_login_at      │
└─────────┬──────────┘
          │ 1
          │
          │ N
┌─────────▼──────────┐        N          1        ┌────────────────────┐
│ user_books         │────────────────────────────►│ books              │
│--------------------│                             │--------------------│
│ PK user_book_id    │                             │ PK book_id         │
│ FK user_id         │                             │ source             │
│ FK book_id         │                             │ external_id        │
│ status             │                             │ title              │
│ rating             │                             │ authors            │
│ review             │                             │ description        │
│ added_at           │                             │ categories         │
│ started_at         │                             │ published_date     │
│ finished_at        │                             │ page_count         │
│ last_reading_url   │                             │ language           │
│ last_scroll_y      │                             │ thumbnail_url      │
│ updated_at         │                             │ preview_link       │
└─────────┬──────────┘                             │ created_at         │
          │ 1                                      │ updated_at         │
          │ N                                      └────────────────────┘
┌─────────▼──────────┐
│ reading_sessions   │
│--------------------│
│ PK session_id      │
│ FK user_book_id    │
│ read_date          │
│ minutes_read       │
│ pages_read         │
│ note               │
│ created_at         │
└────────────────────┘

┌────────────────────┐
│ recommendation_cache│
│--------------------│
│ PK/FK user_id      │
│ cache_key          │
│ books_json         │
│ cached_at          │
└────────────────────┘
```

В Room поля `authors` и `categories` хранятся как списки строк через `AppConverters`.

Документы по БД:

```text
docs/database_schema.md
docs/database_schema.sql
docs/classes_overview.md
```

Файл `docs/database_schema.sql` содержит расширенную проектную SQL-схему для пояснительной записки. Она подробнее нормализует авторов, жанры, источники и рекомендации. Текущая Android-реализация использует более компактную Room-схему, показанную выше.

Файл `docs/classes_overview.md` содержит описание основных классов, экранов, репозиториев, DAO и моделей проекта.

## Как посмотреть БД

Через Android Studio:

```text
View -> Tool Windows -> App Inspection -> Database Inspector
```

Выбрать процесс:

```text
com.finenkodenis.bookshelf
```

Затем открыть:

```text
bookshelf.db
```

Через adb:

```powershell
& "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe" shell run-as com.finenkodenis.bookshelf ls databases
```

Скопировать БД на компьютер:

```powershell
& "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe" exec-out run-as com.finenkodenis.bookshelf cat databases/bookshelf.db > bookshelf.db
```

## Установка и запуск

1. Открыть проект в Android Studio.
2. Дождаться Gradle Sync.
3. Выбрать эмулятор или Android-устройство.
4. Запустить конфигурацию `app`.

Минимальная версия Android:

```text
minSdk = 25
```

Целевая версия:

```text
targetSdk = 33
compileSdk = 34
```

## Команды Gradle

Сборка debug APK:

```powershell
.\gradlew.bat :app:assembleDebug
```

Запуск unit-тестов:

```powershell
.\gradlew.bat test
```

Сборка и тесты:

```powershell
.\gradlew.bat :app:assembleDebug test
```

Установка на подключенный эмулятор или устройство:

```powershell
.\gradlew.bat :app:installDebug
```

## Тесты

Unit-тесты находятся здесь:

```text
app/src/test/java/com/finenkodenis/bookshelf
```

Покрыты:

- конвертеры Room;
- хеширование паролей;
- рекомендательная система;
- fallback-каталог книг;
- преобразование ответов Open Library, Gutendex, Internet Archive и Library of Congress в модель `Book`;
- устойчивость режима `Все`, когда один из внешних API временно недоступен;
- обработка URL обложек;
- расчет времени чтения;
- демо-данные для статистики и рекомендаций.

Запуск:

```powershell
.\gradlew.bat :app:testDebugUnitTest
```

## Основные сценарии проверки

### Демо-вход

1. Запустить приложение.
2. Нажать **"Войти как демо-пользователь"**.
3. Перейти в **"Статистика"**.
4. Проверить, что отображаются книги, минуты, страницы и календарь за 30 дней.

### Поиск книг

1. Открыть **"Поиск"**.
2. Ввести название, автора или жанр.
3. Нажать **"Найти"**.
4. Открыть карточку книги.
5. Добавить книгу в библиотеку.

### Проверка рекомендаций

1. Войти демо-пользователем.
2. Открыть **"Рекомендации"**.
3. Проверить строку основных тем.
4. Нажать **"Обновить"**, если нужно повторно загрузить подборку.

### Проверка чтения

1. Открыть книгу.
2. Нажать **"Читать в приложении"**.
3. На экране чтения нажать **"Назад"**.
4. В диалоге нажать **"Засчитать"**.
5. Проверить, что книга получила статус `Читаю`, а статистика обновилась.

## Ограничения

- Полный текст книги доступен только если его предоставляет внешний источник.
- Внешние API могут временно возвращать пустые ответы, 429 или 503. Для этого в приложении есть fallback-каталог.
- Демо-данные предназначены для проверки интерфейса, статистики и рекомендаций, а не для реального каталога.
- Текущие рекомендации основаны на частоте жанров, без ML-модели.

## Связь с требованиями диплома

| Требование | Реализация |
|---|---|
| Хранение книг и пользователей | Room/SQLite, таблицы `users`, `books`, `user_books`, `reading_sessions` |
| Статусы книг | `WANT_TO_READ`, `READING`, `READ`, `PAUSED`, `DROPPED` |
| Рекомендации | `RecommendationEngine`, подбор по жанрам прочитанных книг |
| Личный кабинет | Экран библиотеки, фильтрация по статусам, экран статистики |
| Графики и календарь | Статистика по статусам, календарь чтения за 30 дней, жанровая диаграмма |
| Экран книги | `BookDetailScreen`, описание, жанры, источник, статус, оценка, отзыв |
| Интеграции | Open Library API, Gutendex API, Internet Archive API, Library of Congress API, fallback-каталог |
| Чтение внутри приложения | `ReaderScreen` со встроенным WebView |

## Репозиторий

```text
https://github.com/MaximillianVoss/BookShelf.git
```
